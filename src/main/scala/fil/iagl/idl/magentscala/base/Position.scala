package fil.iagl.idl.magentscala.base

/**
  * A position of an [[fil.iagl.idl.magentscala.base.Agent]] is represented by two coordinates : the abscissa and the ordinate.
  * @param x the abscissa
  * @param y the ordinate
  */
case class Position(x: Int, y: Int) {}
